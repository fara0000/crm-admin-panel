import React, { FC } from 'react';
import './index.scss';

export const ActivesPage: FC = () => (
        <div className='actives-page-wrapper'>
            {'Actives Page'}
        </div>
)

export default ActivesPage;